= pogic DEV_NOTES
== TODO
+ processing of `pogic.txt` files
+ double check that reset and refresh interfaces are correct.

== Wishlist
+ Track a `.pogic.toml.bak` after builds?
+ What happens when a clone fails?
  + `--continue`
+ `cmd-check <FILE>`
+ `pre-build-cmd`
+ `post-build-cmd`
+  Migrate to https://docs.rs/git2/0.20.4/git2 library.
