= TODO
+ "repos with expected updates" subcommandLook into fleshing out subcommands.
  + something like `pogic updating`
    ```
    repo1         0.1.0 => 0.2.0
    repo2         0.2.0 => 0.2.1
    ```
+ Look into fleshing out subcommands.
